# Force devDependencies to install even when NODE_ENV=production is set in the
# environment. Without this, `npm install` silently omits devDependencies
# (vite, @vitejs/plugin-react, etc.), producing a broken install where the dev
# server can't render. This keeps installs correct regardless of the shell env.
include=dev
